/**
 * Скрипт для запуска приложения UniFarm через Replit
 * 
 * Этот файл должен использоваться как точка входа для запуска приложения
 * в Replit с помощью кнопки "Run" в интерфейсе Replit.
 */

// Запускаем основной скрипт запуска приложения
console.log('🚀 Запуск UniFarm через Replit...');
console.log('⏱️ Время запуска:', new Date().toISOString());

// Импортируем child_process для запуска нашего скрипта
const { spawn } = require('child_process');

// Запускаем start.cjs как отдельный процесс
const serverProcess = spawn('node', ['start.cjs'], {
  stdio: 'inherit' // Перенаправляем ввод/вывод в родительский процесс
});

// Обрабатываем завершение дочернего процесса
serverProcess.on('close', (code) => {
  console.log(`⚠️ Сервер завершил работу с кодом ${code}`);
  
  // Не завершаем процесс, а создаем бесконечный цикл
  setInterval(() => {
    // Этот интервал будет поддерживать процесс активным
  }, 1000);
});

// Обрабатываем ошибки дочернего процесса
serverProcess.on('error', (err) => {
  console.error(`❌ Ошибка запуска сервера: ${err}`);
});

// Обрабатываем необработанные исключения
process.on('uncaughtException', (err) => {
  console.error('❌ Необработанное исключение в run.cjs:', err);
  // Не завершаем процесс
});